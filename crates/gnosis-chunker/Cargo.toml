[package]
name = "chunker"
version = "0.1.0"
edition = "2024"
description = "Markdown chunking shared across gnosis front-ends"

[dependencies]
pulldown-cmark = { workspace = true }
serde = { workspace = true }
