[package]
name = "chunker"
version = "0.1.0"
edition = "2024"
description = "Local semantic search and related-notes for a markdown knowledge base"

[dependencies]
pulldown-cmark = { workspace = true }
